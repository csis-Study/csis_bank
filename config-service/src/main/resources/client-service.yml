server:
  port: 8084

spring:
  application:
    name: client-service
  datasource:
    url: jdbc:mysql://db-server:3306/client_db?useSSL=false
    username: client_user
    password: Client@123

kafka:
  bootstrap-servers: kafka-server:9092
  topic: client-events